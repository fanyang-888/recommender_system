This directory stores local data files and generated outputs.

- Put raw dataset files into `data_raw/`.
- Generated temporary/recommendation files can go into `temp_results/`.

Both folders are ignored by Git in this repository.
